######
# Configuracion Kubernetes Config
######

spring:
  config:
    import: "kubernetes:"
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - name: ms-concepto-jdbc
          - name: bbdd
          - name: osb
          - name: rest
          - name: context-shared
